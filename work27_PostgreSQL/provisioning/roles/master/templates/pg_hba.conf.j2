# TYPE  DATABASE        USER                     ADDRESS                 METHOD
local   all             all                                              peer
host    all             all                      {{ allowed_network }}   trust
host    replication     {{ streaming_username }} {{ allowed_network }}   trust
