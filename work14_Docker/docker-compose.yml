version: '3'
#networks:
# php:

services:
 web:
  build: .
  container_name: "nginx_kos"
#  depends_on:
#   - php
  ports:
    - "80:80"
#  networks:
#    - php
  volumes:
    - ./www:/var/www/
    - ./logs/nginx:/var/log/nginx

 php:
  build: Dockerfile1
  container_name: "php_kos"
  ports:
    - "9000:9000"
  volumes:
    - ./www:/var/www/